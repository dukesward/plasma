import plasma.basic as basic
import plasma.statistics as stats
import plasma.visual as vis

def test():
	# d = basic.Dot(1, 2)
	# v = basic.Set(d)
	# v.extend(basic.Dot(3, 2))
	# print(v)
	# vis.init()

	m = basic.Matrix(1, 6, 'x+1')
	print(m)

	return